name: Health check

on:
  schedule:
    - cron: "*/2 * * * *"

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: check deployed URL
        uses: jtalk/url-health-check-action@v4
        with:
          url: https://full-stack-open-pokedex-zhangtdavid.fly.dev/